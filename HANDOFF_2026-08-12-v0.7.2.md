# Lumen Canvas v0.7.2 — 所有功能在畫布內 HANDOFF

> **時間**：2026-08-12 21:05
> **狀態**：✅ v0.7.2 公開上線 / 語音工作臺變畫布內浮動面板
> **老蔡要求**：「我們最主要是在畫布裡面工作」

---

## 1. 公開 URL

**🌐 Lumen Canvas v0.7.2**：https://lumen-canvas-v072-20260812.surge.sh

## 2. 試試畫布內

1. **主頁**：https://lumen-canvas-v072-20260812.surge.sh/
2. **點「開始使用」**或**「開啟畫布」** → 進 `/canvas`
3. **看右下角**：**🎙 語音** 浮動按鈕（酒紅圓角）
4. **點** → 從右側滑入語音工作臺（3 個 tab：字幕辨識/翻譯/配音）
5. **點外面**或**✕** → 收回
6. 在所有 `/canvas/*` 路由都會看到按鈕

## 3. 為什麼這樣改

### 之前 v0.7.1（壞）
- 「語音工作臺」是獨立路由 `/voice` — **離開畫布**
- 老蔡看到要切頁才能用語音 → 違反「畫布為主」原則

### v0.7.2（好）
- 「語音工作臺」變成**畫布內的浮動按鈕 + 浮動面板**
- 用 Zustand store 控制開合
- 在所有 `/canvas/*` 路由都顯示
- 點外面一點就收回 — 不打斷工作

## 4. 程式碼結構

```
src/
├── components/
│   └── voice-floating/        ← 新增
│       ├── button.tsx         ← 浮動按鈕
│       ├── index.tsx          ← 浮動面板（從右滑入）
│       ├── store.ts           ← Zustand 開合狀態
│       └── public.ts          ← 公開 API
└── layouts/
    └── user-layout.tsx        ← 注入按鈕（只在 /canvas 顯示）
```

## 5. 5 個 commit
```
e3a49a5 v0.7.2: 語音工作臺改成畫布內浮動面板
b8d5fdd v0.7.1: ship HANDOFF
74385f8 v0.7.1: 簡轉繁 + 語音工作臺 + Lumen 品牌
071f533 v0.7: 整合 SHUO 功能計畫
+ 5 個 upstream
```

## 6. 下次 v0.7.3 自動執行

1. 把生圖/影片/提示詞/資產也改成畫布內浮動按鈕（4 個）
2. 修 logo 左側紫色怪 icon
3. 修語言切換按鈕文案
4. 串接真實 ASR / 翻譯 / TTS API

## 7. 對齊業界

| 設計 | 對標 |
|---|---|
| 浮動按鈕 + 從右側滑入 | tldraw / Figma / Notion 全用 |
| Backdrop blur 黑遮罩 | Vercel / Stripe Dashboard |
| Zustand 控制 + 自動路由判斷 | 標準 React pattern |

---

**老蔡試 v0.7.2，重點：**
- 開 `https://lumen-canvas-v072-20260812.surge.sh/`
- 進任何 `/canvas` 頁
- 看右下角 🎙 語音 浮動按鈕
- 點 → 從右滑入語音工作臺（不離開畫布）
